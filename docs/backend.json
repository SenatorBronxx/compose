
{
  "entities": {
    "DriverApplication": {
      "title": "Driver Application",
      "description": "Represents a driver's application to join the service.",
      "type": "object",
      "properties": {
        "fullName": {
          "type": "string",
          "description": "The full name of the applicant."
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "The email address of the applicant."
        },
        "licenseNumber": {
          "type": "string",
          "description": "The applicant's driver's license number."
        },
        "ghanaCardNumber": {
          "type": "string",
          "description": "The applicant's Ghana Card number."
        }
      },
      "required": [
        "fullName",
        "email",
        "licenseNumber",
        "ghanaCardNumber"
      ]
    }
  },
  "auth": {
    "providers": [
      "anonymous"
    ]
  },
  "firestore": {
    "/driver-applications/{applicationId}": {
      "schema": {
        "$ref": "#/backend/entities/DriverApplication"
      },
      "description": "Stores all driver applications submitted through the website."
    }
  }
}
